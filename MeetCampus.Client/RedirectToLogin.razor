@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/auth/login?returnUrl={Uri.EscapeDataString(new Uri(NavigationManager.Uri).PathAndQuery)}");
    }
}
